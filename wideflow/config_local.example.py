# Per-machine storage paths — copy this file to config_local.py (gitignored) and edit the
# values below to match where claustrum-storage / the data staging drive actually live on
# this machine, then leave config_local.py alone (never commit it).
# added by Claude 20260906

BASE_PATH = "/claustrum-storage/pblab_shared_data/Lena/WideFlow_prj"
DATA_STAGING_PATH = "/data/Lena/WideFlow_prj"
